package com.example.todoapp.data.repository

/*
* Provider for repository to represent to worker and connectionManager
*/
object RepositoryProvider {
    lateinit var repository: TodoItemsRepositoryImpl
}